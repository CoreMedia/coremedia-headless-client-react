fragment CMProductTeaser on CMProductTeaser {
  ...CMTeasable
  productRef {
    ...ProductRef
  }
  shopNowSetting: settings(paths: ["shopNow"])
  ctaSettings: settings(paths: ["callToActionDisabled", "callToActionCustomText", "callToActionStyle"])
}
